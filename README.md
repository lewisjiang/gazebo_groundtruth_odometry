# gazebo groundtruth odometry
Record real time pose, path, and path length(aka mileage) from "gazebo/model_states", to show in rviz.  
TODO  
- [ ] Add tf to show pose graph and path in the same view.
